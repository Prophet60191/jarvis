const express = require('express');
const app = express();
const port = 3000;

app.use(express.static('public'));

app.get('/weather', (req, res) => {
    // TO DO: implement API call to get weather data
    res.send({ message: 'Weather data coming soon!' });
});

app.listen(port, () => {
    console.log(`Server started on port ${port}`);
});
